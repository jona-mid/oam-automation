pandas
requests
earthengine-api
tqdm
numpy
rasterio
xarray
zarr
Pillow
pydantic
python-dotenv
# Not on PyPI: install deadtrees-cli (and the `shared` package it brings)
# from the deadtrees monorepo into the same environment.
